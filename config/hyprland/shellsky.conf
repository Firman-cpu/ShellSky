# ShellSky integration
# This file is sourced by Hyprland during installation.

$SHELLSKY_DIR = ~/.config/quickshell/shellsky

# Start the installed ShellSky configuration once with Hyprland.
exec-once = $SHELLSKY_DIR/scripts/run.sh --installed
